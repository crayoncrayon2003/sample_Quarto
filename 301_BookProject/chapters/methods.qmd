# 手法 {#sec-methods}

2番目の本文章です。数式・表・引用など、これまでのサンプルで扱った
要素はそのまま book プロジェクトでも使えます。

## 数式

$$
y = ax + b
$$ {#eq-linear}

@eq-linear は単回帰モデルの式です。

## 引用

参考文献も `_quarto.yml` の `bibliography:` で一元管理されます
[@knuth1984]。

## 前の章へ

導入は @sec-intro を参照してください。

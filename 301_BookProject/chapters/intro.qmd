# 導入 {#sec-intro}

この章は book プロジェクトの最初の本文章です。
`_quarto.yml` の `chapters:` に列挙された順に並びます。

## 章ファイルの書き方

- 各章ファイルの先頭は見出し `#`（章タイトル）から始める
- フロントマターは基本的に不要（プロジェクト設定が適用される）
- `{#sec-...}` ラベルを付ければ他章から参照できる

## 他章への参照

詳しくは @sec-methods（手法の章）を参照してください。
このように章をまたいで相互参照できます。
